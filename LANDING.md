# Сборка лендинга
- Важно реализовать все компоненты таким образом чтобы можно было строить полноценный лендинг из готовых компонентов

## Пример 
- Пример реализации тсраниц

```javascript
<Layout>
    <Header props:={} />
    <Hero props:={} />
    <Categories props:={} />
    <Carousel props:={} />
    <Faqs props:={} />
    <Footer props:={} />
</Layout>
```